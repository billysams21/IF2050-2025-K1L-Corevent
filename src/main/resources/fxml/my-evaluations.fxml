<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.*?>
<?import javafx.geometry.Insets?>

<VBox xmlns:fx="http://javafx.com/fxml" fx:controller="com.corevent.controller.MyEvaluationsController" 
      spacing="20" styleClass="my-evaluations-container">
    <padding>
        <Insets top="30" right="30" bottom="30" left="30"/>
    </padding>

    <!-- Header -->
    <VBox spacing="10" styleClass="evaluations-header">
        <Label text="My Evaluations" styleClass="form-title"/>
        <Label text="List of events you can or have evaluated" styleClass="subtitle"/>
        <Separator/>
    </VBox>

    <!-- Statistics -->
    <HBox spacing="20" styleClass="statistics-container">
        <VBox styleClass="stat-card" HBox.hgrow="ALWAYS">
            <Label text="Completed Events" styleClass="stat-label"/>
            <Label fx:id="completedEventsLabel" text="0" styleClass="stat-value"/>
        </VBox>
        <VBox styleClass="stat-card" HBox.hgrow="ALWAYS">
            <Label text="Submitted Evaluations" styleClass="stat-label"/>
            <Label fx:id="submittedEvaluationsLabel" text="0" styleClass="stat-value"/>
        </VBox>
        <VBox styleClass="stat-card" HBox.hgrow="ALWAYS">
            <Label text="Pending Evaluations" styleClass="stat-label"/>
            <Label fx:id="pendingEvaluationsLabel" text="0" styleClass="stat-value"/>
        </VBox>
    </HBox>

    <!-- Filter Tabs -->
    <TabPane fx:id="evaluationTabPane" VBox.vgrow="ALWAYS" styleClass="evaluation-tabs">
        
        <!-- Tab for events that can be evaluated -->
        <Tab text="Evaluable" closable="false">
            <VBox spacing="15">
                <HBox alignment="CENTER_LEFT" spacing="10">
                    <Label text="Events You Can Evaluate" styleClass="section-title"/>
                    <Region HBox.hgrow="ALWAYS"/>
                    <Button fx:id="refreshAvailableButton" text="Refresh" onAction="#handleRefreshAvailable" styleClass="button-secondary"/>
                </HBox>
                
                <TableView fx:id="availableEventsTable" VBox.vgrow="ALWAYS">
                    <columns>
                        <TableColumn fx:id="availableEventNameColumn" text="Event Name" prefWidth="250"/>
                        <TableColumn fx:id="availableDateColumn" text="Date" prefWidth="150"/>
                        <TableColumn fx:id="availableLocationColumn" text="Location" prefWidth="200"/>
                        <TableColumn fx:id="availableStatusColumn" text="Status" prefWidth="120"/>
                        <TableColumn fx:id="availableActionsColumn" text="Action" prefWidth="150"/>
                    </columns>
                    <placeholder>
                        <Label text="No events available for evaluation at the moment" styleClass="table-placeholder"/>
                    </placeholder>
                </TableView>
            </VBox>
        </Tab>
        
        <!-- Tab for submitted evaluations -->
        <Tab text="Submitted" closable="false">
            <VBox spacing="15">
                <HBox alignment="CENTER_LEFT" spacing="10">
                    <Label text="Submitted Evaluations" styleClass="section-title"/>
                    <Region HBox.hgrow="ALWAYS"/>
                    <Button fx:id="refreshSubmittedButton" text="Refresh" onAction="#handleRefreshSubmitted" styleClass="button-secondary"/>
                </HBox>
                
                <TableView fx:id="submittedEvaluationsTable" VBox.vgrow="ALWAYS">
                    <columns>
                        <TableColumn fx:id="submittedEventNameColumn" text="Event Name" prefWidth="250"/>
                        <TableColumn fx:id="submittedScoreColumn" text="Rating" prefWidth="100"/>
                        <TableColumn fx:id="submittedFeedbackColumn" text="Comment" prefWidth="300"/>
                        <TableColumn fx:id="submittedDateColumn" text="Submitted Date" prefWidth="150"/>
                        <TableColumn fx:id="submittedActionsColumn" text="Action" prefWidth="100"/>
                    </columns>
                    <placeholder>
                        <Label text="No evaluations submitted yet" styleClass="table-placeholder"/>
                    </placeholder>
                </TableView>
            </VBox>
        </Tab>
    </TabPane>

    <!-- Action Buttons -->
    <HBox spacing="15" styleClass="action-buttons" alignment="CENTER_RIGHT">
        <Button text="Back to Dashboard" onAction="#handleBackToDashboard" styleClass="button-primary" prefWidth="180"/>
    </HBox>
</VBox>
